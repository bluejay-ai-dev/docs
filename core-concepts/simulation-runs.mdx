---
title: 'Simulation Runs'
description: 'Learn about the format and lifecycle of Bluejay simulation runs'
icon: play
---

A simulation run is a single execution of a configured [simulation](/test/simulations/overview). Each run captures the full set of interactions between your voice agent and the digital humans defined in the simulation, producing a structured record you can review, compare, and act on.

## What Is a Simulation Run?

A **Simulation Run** represents one execution instance of a specific simulation scenario. When you initiate a simulation run, Bluejay's digital humans engage your voice agent through realistic phone call interactions. The run tracks every call from start to finish and records the data needed to evaluate performance.

## Run Lifecycle

Every simulation run moves through a predictable set of stages:

1. **Queued** — The run has been submitted and is waiting for available capacity.
2. **In Progress** — Digital humans are actively calling your voice agent and interactions are underway.
3. **Completed** — All calls in the run have finished and results are ready for review.
4. **Failed** — The run could not complete due to a configuration or connectivity issue.

## Run Structure

A simulation run is composed of the individual calls placed by each digital human in the simulation. The run record includes:

- **Run ID** — A unique identifier for referencing this execution.
- **Simulation Reference** — The parent simulation the run belongs to.
- **Timestamp** — When the run was initiated.
- **Call Count** — The total number of calls placed during the run.
- **Status** — The current lifecycle stage of the run (queued, in progress, completed, or failed).

## Initiating a Run

You can start a simulation run from the Bluejay dashboard or programmatically through the API:

- **Dashboard** — Navigate to your simulation and click **Run Simulation**.
- **API** — Use the [Queue Simulation Run](/api-reference/endpoint/queue-simulation-run) endpoint to trigger a run for voice simulations, or [Queue SMS Simulation Run](/api-reference/endpoint/queue-sms-simulation-run) for SMS-based scenarios.

## Best Practices

- **Run frequently** — Execute simulation runs on a regular cadence so you can detect regressions early.
- **Compare across runs** — Use run timestamps and IDs to track how your agent's behavior evolves over successive executions.
- **Automate via API** — Integrate simulation runs into your CI/CD pipeline or scheduled workflows to keep testing continuous.

## Example Run Record

```md
Run ID: run_abc123
Simulation: "Billing Support Stress Test"
Status: Completed
Queued At: 2025-06-14T10:30:00Z
Completed At: 2025-06-14T10:42:15Z
Total Calls: 15
