---
title: 'Update Workflow'
description: ''
openapi: 'PUT /v1/update-workflow/{workflow_id}'
---
<Accordion title="Copy AI Integration Prompt">
````
# Integrate Update Workflow — PUT /v1/update-workflow/{workflow_id}

**You are a senior backend engineer. Your task: integrate the endpoint below into the existing codebase with minimal, surgical changes. Do not refactor, rename, or restructure anything beyond what is required for this integration.**

## Endpoint Details
- **Method:** PUT
- **URL:** `https://api.getbluejay.ai/v1/update-workflow/{workflow_id}`
- **Auth:** API Key via `X-API-Key` header
- **Content-Type:** application/json

## Parameters
| Name | Location | Type | Required | Description |
|------|----------|------|----------|-------------|
| workflow_id | path | string | yes |  |
| X-API-Key | header | string | yes | API key required to authenticate requests. |
| name | body | string (nullable) | no | New name for the workflow |
| workflow | body | object (nullable) | no | Updated workflow structure |
| agent_ids | body | array[integer] (nullable) | no | Updated list of agent IDs this workflow belongs to (can be empty to remove all associations) |
| enabled_for_evaluation | body | boolean (nullable) | no | Whether this workflow should be used for agent evaluation |

## Request Body Schema
```json
{
  "name": "string",
  "workflow": {
    "nodes": [
      {
        "id": "string",
        "type": "start",
        "position": {
          "x": 1.0,
          "y": 1.0
        },
        "data": {
          "label": "string",
          "description": "string"
        }
      }
    ],
    "edges": [
      {
        "id": "string",
        "source": "string",
        "target": "string",
        "label": "string",
        "type": "string"
      }
    ]
  },
  "agent_ids": [
    123
  ],
  "enabled_for_evaluation": true
}
```

## Response
- **Success (200):**
```json
{
  "id": "string",
  "name": "example_name",
  "workflow": {
    "nodes": [
      {
        "id": "string",
        "type": "start",
        "position": {
          "x": 1.0,
          "y": 1.0
        },
        "data": {
          "label": "string",
          "description": "string"
        }
      }
    ],
    "edges": [
      {
        "id": "string",
        "source": "string",
        "target": "string",
        "label": "string",
        "type": "string"
      }
    ]
  },
  "enabled_for_evaluation": true,
  "created_at": "string",
  "updated_at": "2024-01-01T00:00:00Z",
  "agent_ids": [
    123
  ]
}
```
- **Key error codes:** 422: Validation Error

## Integration Instructions
1. Create or locate the appropriate service/module for this API domain.
2. Implement the API call using the project's existing HTTP client and patterns.
3. Include proper error handling for the documented error codes.
4. Add TypeScript types / interfaces for request params and response shape (if TS project).
5. Export the function so it's consumable by the rest of the codebase.

## Few-Shot Example
**PUT with body:**
```python
import requests

def update_workflow(workflow_id: str, payload: dict, api_key: str) -> dict:
    url = f"https://api.getbluejay.ai/v1/update-workflow/{workflow_id}"
    headers = {"X-API-Key": api_key}
    response = requests.put(url, headers=headers, json=payload)
    response.raise_for_status()
    return response.json()
```

## Constraints
- **Be concise.** Only add/change files directly needed for this integration.
- **Match existing patterns** in the codebase (naming, file structure, error handling).
- **Do not** install new dependencies unless absolutely required.
- **Do not** modify unrelated files.

**Reminder: Integrate this single endpoint fully and correctly. Minimal diff, maximum correctness.**
````
</Accordion>


Given the workflow ID and, optionally, the name and workflow structure, this endpoint updates your workflow and returns the updated workflow.
