---
title: 'Get Workflow'
description: ''
openapi: 'GET /v1/workflow/{workflow_id}'
---

This page documents **GET** `/v1/workflow/{workflow_id}` for the current React Flow–based workflow API. Prefer the primary reference: [Get workflow](/api-reference/endpoint/get-workflow-v2).

Given the workflow ID, this endpoint returns the workflow row including the `definition` graph.
