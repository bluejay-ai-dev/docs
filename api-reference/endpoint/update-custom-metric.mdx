---
title: 'Update Custom Metric'
description: ''
openapi: 'PUT /v1/update-custom-metric/{metric_id}'
---
<Accordion title="Copy AI Integration Prompt">
````
# Integrate Update Custom Metric — PUT /v1/update-custom-metric/{metric_id}

**You are a senior backend engineer. Your task: integrate the endpoint below into the existing codebase with minimal, surgical changes. Do not refactor, rename, or restructure anything beyond what is required for this integration.**

## Endpoint Details
- **Method:** PUT
- **URL:** `https://api.getbluejay.ai/v1/update-custom-metric/{metric_id}`
- **Auth:** API Key via `X-API-Key` header
- **Content-Type:** application/json

## Parameters
| Name | Location | Type | Required | Description |
|------|----------|------|----------|-------------|
| metric_id | path | string | yes |  |
| X-API-Key | header | string | yes | API key required to authenticate requests. |
| name | body | string (nullable) | no | Name of the custom metric |
| description | body | string (nullable) | no | Description of what this metric measures |
| response_type | body | string (enum: pass_fail, yes_no, qualitative, quantitative, json, enum) (nullable) | no | Type of response expected |
| agent_ids | body | array[integer] (nullable) | no | List of agent IDs to associate this metric with (replaces existing associations) |
| min_value | body | number (nullable) | no | Minimum value for quantitative metrics |
| max_value | body | number (nullable) | no | Maximum value for quantitative metrics |
| category | body | string (nullable) | no | Category for organizing metrics |
| tags | body | array[string] (nullable) | no | Tags for categorizing the metric |
| scoring_guidance | body | string (nullable) | no | Guidance on how to score this metric |
| eval_route | body | string (enum: AUDIO, TEXT, AUTO) (nullable) | no | Route for evaluating the metric (default: AUTO) |
| model | body | string (nullable) | no | Model to use for evaluating the metric |
| temperature | body | number (nullable) | no | Temperature for evaluating the metric |
| enum_options | body | array[string] (nullable) | no | Options for enum metrics |
| allow_not_applicable | body | boolean (nullable) | no | Whether this metric allows 'not applicable' responses. |

## Request Body Schema
```json
{
  "name": "string",
  "description": "string",
  "response_type": "pass_fail",
  "agent_ids": [
    123
  ],
  "min_value": 1.0,
  "max_value": 1.0,
  "category": "string",
  "tags": [
    "string"
  ],
  "scoring_guidance": "string",
  "eval_route": "AUDIO",
  "model": "string",
  "temperature": 1.0,
  "enum_options": [
    "string"
  ],
  "allow_not_applicable": true
}
```

## Response
- **Success (200):**
```json
{
  "id": "string",
  "name": "example_name",
  "description": "string",
  "response_type": "pass_fail",
  "agent_ids": [
    123
  ],
  "min_value": 1.0,
  "max_value": 1.0,
  "category": "string",
  "tags": [
    "string"
  ],
  "scoring_guidance": "string",
  "created_at": "string",
  "eval_route": "AUDIO",
  "model": "string",
  "temperature": 1.0,
  "enum_options": [
    "string"
  ],
  "allow_not_applicable": false
}
```
- **Key error codes:** 422: Validation Error

## Integration Instructions
1. Create or locate the appropriate service/module for this API domain.
2. Implement the API call using the project's existing HTTP client and patterns.
3. Include proper error handling for the documented error codes.
4. Add TypeScript types / interfaces for request params and response shape (if TS project).
5. Export the function so it's consumable by the rest of the codebase.

## Few-Shot Example
**PUT with body:**
```python
import requests

def update_custom_metric(metric_id: str, payload: dict, api_key: str) -> dict:
    url = f"https://api.getbluejay.ai/v1/update-custom-metric/{metric_id}"
    headers = {"X-API-Key": api_key}
    response = requests.put(url, headers=headers, json=payload)
    response.raise_for_status()
    return response.json()
```

## Constraints
- **Be concise.** Only add/change files directly needed for this integration.
- **Match existing patterns** in the codebase (naming, file structure, error handling).
- **Do not** install new dependencies unless absolutely required.
- **Do not** modify unrelated files.

**Reminder: Integrate this single endpoint fully and correctly. Minimal diff, maximum correctness.**
````
</Accordion>


This endpoint allows you to update an existing custom metric for your agents. Provide the metric ID and updated details to update the metric.
