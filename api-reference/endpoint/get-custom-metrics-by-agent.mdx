---
title: 'Get Custom Metrics by Agent'
description: ''
openapi: 'GET /v1/custom-metrics-by-agent/{agent_id}'
---
<Accordion title="Copy AI Integration Prompt">
````
# Integrate Get Custom Metrics by Agent — GET /v1/custom-metrics-by-agent/{agent_id}

**You are a senior backend engineer. Your task: integrate the endpoint below into the existing codebase with minimal, surgical changes. Do not refactor, rename, or restructure anything beyond what is required for this integration.**

## Endpoint Details
- **Method:** GET
- **URL:** `https://api.getbluejay.ai/v1/custom-metrics-by-agent/{agent_id}`
- **Auth:** API Key via `X-API-Key` header

## Parameters
| Name | Location | Type | Required | Description |
|------|----------|------|----------|-------------|
| agent_id | path | integer | yes |  |
| X-API-Key | header | string | yes | API key required to authenticate requests. |

## Response
- **Success (200):**
```json
{
  "metrics": [
    {
      "id": "string",
      "name": "example_name",
      "description": "string",
      "response_type": "pass_fail",
      "agent_ids": [
        123
      ],
      "min_value": 1.0,
      "max_value": 1.0,
      "category": "string",
      "tags": [
        "string"
      ],
      "scoring_guidance": "string",
      "created_at": "string",
      "eval_route": "AUDIO",
      "model": "string",
      "temperature": 1.0,
      "enum_options": [
        "string"
      ],
      "allow_not_applicable": false
    }
  ],
  "total_count": 123,
  "page": 123,
  "page_size": 123,
  "items_in_page": 123,
  "total_pages": 123
}
```
- **Key error codes:** 422: Validation Error

## Integration Instructions
1. Create or locate the appropriate service/module for this API domain.
2. Implement the API call using the project's existing HTTP client and patterns.
3. Include proper error handling for the documented error codes.
4. Add TypeScript types / interfaces for request params and response shape (if TS project).
5. Export the function so it's consumable by the rest of the codebase.

## Few-Shot Example
**Simple GET:**
```python
import requests

def get_custom_metrics_by_agent(agent_id: int, api_key: str) -> dict:
    url = f"https://api.getbluejay.ai/v1/custom-metrics-by-agent/{agent_id}"
    headers = {"X-API-Key": api_key}
    response = requests.get(url, headers=headers)
    response.raise_for_status()
    return response.json()
```

## Constraints
- **Be concise.** Only add/change files directly needed for this integration.
- **Match existing patterns** in the codebase (naming, file structure, error handling).
- **Do not** install new dependencies unless absolutely required.
- **Do not** modify unrelated files.

**Reminder: Integrate this single endpoint fully and correctly. Minimal diff, maximum correctness.**
````
</Accordion>


This endpoint allows you to retrieve all custom metrics associated with a specific agent. Provide the agent ID in the path to get a list of all custom metrics configured for that agent.

**DEPRECATED**: This endpoint is deprecated. Please use `GET /custom-metrics?agent_id={agent_id}` instead, which supports pagination and additional features.