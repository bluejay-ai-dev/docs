---
title: 'Generate intent summary'
description: 'Generate an intent summary for a test case when intent exceeds a character limit.'
openapi: 'POST /v1/generate-intent-summary'
---

<div className="ai-prompt-box">
<div className="ai-prompt-box-header">Integration Prompt for AI Agents</div>
````
## Generate intent summary — POST /v1/generate-intent-summary

**Endpoint:** POST `https://api.getbluejay.ai/v1/generate-intent-summary`
**Auth:** `X-API-Key` header

### Constraints
- **400** when the test case is **workflow-tagged** (tag lowercased contains `workflow`): intent summary is not applicable; the digital human relies on **enriched playback**.
- Handle **422** for validation errors.

````
</div>

For workflow-tagged digital humans this route returns **400**; see [Workflow tags & enriched playback](/key-concepts/digital-humans/workflow-and-enriched-playback).
