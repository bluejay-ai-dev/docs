---
title: 'Delete workflow'
description: 'Delete a workflow and its stored typed React Flow graph by UUID.'
openapi: 'DELETE /v1/workflow/{workflow_id}'
---

Successful responses include `message` and `deleted_workflow_id` (UUID of the removed workflow).
