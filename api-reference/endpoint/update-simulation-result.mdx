---
title: 'Update Simulation Result'
description: ''
openapi: 'POST /v1/update-simulation-result'
---
<Accordion title="Copy AI Integration Prompt">
````
# Integrate Update Simulation Result — POST /v1/update-simulation-result

**You are a senior backend engineer. Your task: integrate the endpoint below into the existing codebase with minimal, surgical changes. Do not refactor, rename, or restructure anything beyond what is required for this integration.**

## Endpoint Details
- **Method:** POST
- **URL:** `https://api.getbluejay.ai/v1/update-simulation-result`
- **Auth:** API Key via `X-API-Key` header
- **Content-Type:** application/json

## Parameters
| Name | Location | Type | Required | Description |
|------|----------|------|----------|-------------|
| X-API-Key | header | string | yes | API key required to authenticate requests. |
| simulation_result_id | body | string | yes | Simulation result ID provided via SIP for the specific call |
| events | body | array[object] (nullable) | no | List of events that occurred during the call |
| tool_calls | body | array[object] (nullable) | no | List of tool calls made during the call |
| recorded_transcript | body | array[object] (nullable) | no | Recorded transcript of the call (utilized to analyze STT accuracy) |
| tags | body | array[string] (nullable) | no | List of tags associated with the call |
| metadata | body | object (nullable) | no | Additional metadata associated with the call |

## Request Body Schema
```json
{
  "simulation_result_id": "string",
  "events": [
    {
      "title": "string",
      "start_offset_ms": 123,
      "description": "string",
      "end_offset_ms": 123,
      "tags": [
        "string"
      ],
      "metadata": {
        "key": "value"
      }
    }
  ],
  "tool_calls": [
    {
      "name": "example_name",
      "start_offset_ms": 123,
      "description": "string",
      "parameters": {
        "key": "value"
      }
    }
  ],
  "recorded_transcript": [
    {
      "start_offset_ms": 123,
      "end_offset_ms": 123,
      "speaker": "AGENT",
      "utterance": "string"
    }
  ],
  "tags": [
    "string"
  ],
  "metadata": {
    "key": "value"
  }
}
```

## Response
- **Success (200):**
```json
{
  "status": "success",
  "message": "string"
}
```
- **Key error codes:** 422: Validation Error

## Integration Instructions
1. Create or locate the appropriate service/module for this API domain.
2. Implement the API call using the project's existing HTTP client and patterns.
3. Include proper error handling for the documented error codes.
4. Add TypeScript types / interfaces for request params and response shape (if TS project).
5. Export the function so it's consumable by the rest of the codebase.

## Few-Shot Example
**POST with body:**
```python
import requests

def update_simulation_result(payload: dict, api_key: str) -> dict:
    url = "https://api.getbluejay.ai/v1/update-simulation-result"
    headers = {"X-API-Key": api_key}
    response = requests.post(url, headers=headers, json=payload)
    response.raise_for_status()
    return response.json()
```

## Constraints
- **Be concise.** Only add/change files directly needed for this integration.
- **Match existing patterns** in the codebase (naming, file structure, error handling).
- **Do not** install new dependencies unless absolutely required.
- **Do not** modify unrelated files.

**Reminder: Integrate this single endpoint fully and correctly. Minimal diff, maximum correctness.**
````
</Accordion>


The simulation result ID can be found as a header of the websocket connect message. The header will look like this: 

`X-Simulation-Result-Id: <simulation_result_id>`

This ID associates your tool calls, events, tags, and metadata with the specific simulation result.
Given this ID, this endpoint updates your simulation result to enrich the simulation with the tool calls, events, tags, and metadata you provide.