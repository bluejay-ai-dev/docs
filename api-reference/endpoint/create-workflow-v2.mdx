---
title: 'Create workflow'
description: 'Create a workflow definition as a React Flow graph.'
openapi: 'POST /v1/workflow'
---

This endpoint creates a workflow definition for your organization. Provide a name, optional description, optional agent links, and a React Flow–style graph with `nodes`, `edges`, and optional `viewport`.
